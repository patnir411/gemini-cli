# Gemini Mobile API - Environment Variables
# Copy this file to .env and fill in your values:
#   cp .env.example .env

# Required: Gemini API key from https://aistudio.google.com/apikey
GEMINI_API_KEY=your-gemini-api-key-here

# Required for production: Firebase project for auth
FIREBASE_PROJECT_ID=your-firebase-project-id
# FIREBASE_SERVICE_ACCOUNT={"type":"service_account",...}  # JSON string

# Optional: GCP project (for Vertex AI or GCS storage)
# GOOGLE_CLOUD_PROJECT=your-gcp-project-id

# Optional: Default model (defaults to gemini-2.5-pro)
# DEFAULT_MODEL=gemini-2.5-pro

# Optional: Max concurrent sessions per user (default: 10)
# MAX_SESSIONS_PER_USER=10

# Optional: Session idle timeout in ms (default: 1800000 = 30 minutes)
# SESSION_TIMEOUT_MS=1800000

# Caddy TLS configuration
# For production with a real domain:
#   DOMAIN=gemini-api.yourdomain.com
#   TLS_MODE=
# For local development:
#   DOMAIN=localhost
#   TLS_MODE=internal
DOMAIN=localhost
TLS_MODE=internal
